# 🚀 Быстрое развертывание

## На сервере (первый раз)

```bash
# 1. Подключитесь к серверу
ssh ваш_пользователь@ваш_ip

# 2. Установите Node.js и PM2
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs git
sudo npm install -g pm2

# 3. Клонируйте репозиторий
cd ~
git clone https://github.com/ваш_пользователь/tg-list-bot.git
cd tg-list-bot

# 4. Создайте .env файл
nano .env
# Добавьте: BOT_TOKEN=ваш_токен
# Сохраните: Ctrl+O, Enter, Ctrl+X

# 5. Запустите бота
npm install
npm run build
mkdir -p logs
pm2 start ecosystem.config.js
pm2 save
pm2 startup  # и выполните команду, которую покажет PM2
```

## В GitHub (один раз)

1. Откройте репозиторий → **Settings** → **Secrets and variables** → **Actions**
2. Добавьте три секрета:
   - `VDS_HOST` = IP адрес сервера
   - `VDS_USERNAME` = имя пользователя
   - `VDS_PASSWORD` = пароль

## Готово! 🎉

Теперь при каждом `git push` в ветку `main` бот будет автоматически обновляться на сервере.

Проверить статус:

```bash
pm2 status
pm2 logs tg-list-bot
```
