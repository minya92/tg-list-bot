# Telegram Task List Bot

Telegram бот для отправки списков задач с использованием официального [Telegram Bot API 9.1 Checklists](https://core.telegram.org/bots/api#july-3-2025).

## Особенности

- Использует официальное [Telegram Bot API 9.1](https://core.telegram.org/bots/api#july-3-2025) для чеклистов
- Реализованы типы `InputChecklist` и `InputChecklistTask`
- Прямой вызов метода `sendChecklist` через Telegram Bot API
- Автоматический fallback: если API чеклистов недоступен, отправляется текстовое сообщение с эмодзи чекбоксов (☐)

## ⚠️ Настройка Telegram Business (ВАЖНО!)

### Ошибка: "This bot doesn't support Telegram Business yet"

Если вы видите эту ошибку при попытке подключить бота, следуйте этим шагам:

### 📋 Пошаговая инструкция:

#### 1. Включите Business Mode для бота

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Нажмите **"Bot Settings"**
5. Найдите и включите **"Business Mode"** (или "Allow to be connected to business accounts")

> ⚠️ Если вы не видите эту опцию, возможно она еще не доступна. Попробуйте создать нового бота.

#### 2. Активируйте Telegram Business в настройках

1. Откройте **Telegram** → **Настройки** ⚙️
2. Найдите **"Telegram для бизнеса"** (Telegram Business)
3. Активируйте бизнес-функции
4. Нажмите **"Manage chatbot"** или **"Подключить чат-бота"**
5. Выберите вашего бота из списка

#### 3. Проверьте подключение

Отправьте боту команду `/debug` чтобы проверить статус Business подключения.

### Требования:

- ✅ **Telegram Premium** подписка
- ✅ **Business Account** активирован
- ✅ Бот настроен в **Business Mode** (через BotFather)
- ✅ Бот подключен к вашему Business Account

### 📖 Подробная инструкция

См. файл [BUSINESS_SETUP.md](./BUSINESS_SETUP.md) для детальных инструкций.

## Установка

1. Клонируйте репозиторий:

```bash
git clone <repo-url>
cd tg-list-bot
```

2. Установите зависимости:

```bash
npm install
```

3. Создайте файл `.env` в корне проекта и добавьте токен вашего бота:

```
BOT_TOKEN=your_telegram_bot_token_here
```

## Как получить токен бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания нового бота
4. Скопируйте полученный токен и добавьте его в `.env` файл

## Запуск

### Режим разработки (с автоматической перекомпиляцией):

```bash
npm run dev
```

### Продакшн:

```bash
npm run build
npm start
```

## Использование

### Команды бота:

- `/start` - Приветствие и проверка статуса подключения
- `/debug` - Проверить статус Business подключения
- `/create_list XXX` - Создать список задач (где XXX - номер задачи)

### Пример:

```
/create_list 1234
```

Бот отправит список задач с заголовком:

```
#task 1234. https://sfera-t1.ru/tasks/task/TCOMCLOUD-1234
```

И список из 18 задач для вашего рабочего процесса.

### 💡 Режимы работы:

1. **С Business Account** - нативные чеклисты Telegram (интерактивные)
2. **Без Business Account** - текстовые списки с эмодзи чекбоксов (☐)

## Структура проекта

```
tg-list-bot/
├── src/
│   └── index.ts          # Основной файл бота
├── dist/                 # Скомпилированные файлы (создается после build)
├── .env                  # Переменные окружения (не в репозитории)
├── .gitignore
├── package.json
├── tsconfig.json
└── README.md
```

## Технические детали

### Структура Checklist

Бот использует следующие типы данных из Telegram Bot API:

**InputChecklistTask:**

```typescript
interface InputChecklistTask {
  text: string; // Текст задачи
  checked?: boolean; // Статус: выполнена (true) или нет (false)
}
```

**InputChecklist:**

```typescript
interface InputChecklist {
  title?: string; // Заголовок списка
  tasks: InputChecklistTask[]; // Массив задач
}
```

### API Request

Бот отправляет POST-запрос на:

```
https://api.telegram.org/bot<TOKEN>/sendChecklist
```

С телом:

```json
{
  "chat_id": <chat_id>,
  "checklist": {
    "title": "#task XXX. https://sfera-t1.ru/tasks/task/TCOMCLOUD-XXX",
    "tasks": [
      { "text": "1. Передвинуть задачу в Сфере", "checked": false },
      ...
    ]
  }
}
```

## Список задач

Бот отправляет следующий хардкодированный список задач:

1. Передвинуть задачу в Сфере
2. Создать ветку от релизной
3. Выполнить задачу
4. Обновить сторибук
5. Обновить тесты (npm test -- -u ./src/components/MyComponent)
6. Поднять версии компонентов и сбилдить их
7. Залить компоненты на дев
8. Собрать страницу
9. Проверить версии компонентов и Комит
10. ПР в дев
11. Код-ревью
12. Написать в задаче ссыль на п.3 и версии компонентов
13. Мёрдж в дев
14. Поменять в баге исполнителя либо закрыть задачу
15. Списать время с задачи в Сфере
16. Написать тестеру
17. Создать пр в релизную ветку со статусами тестирование не вливать и проверить название
18. После тестирования поменять статус

## Ссылки

- [Telegram Bot API 9.1 - Checklists](https://core.telegram.org/bots/api#july-3-2025)
- [InputChecklistTask](https://core.telegram.org/bots/api#inputchecklisttask)
- [InputChecklist](https://core.telegram.org/bots/api#inputchecklist)
- [sendChecklist method](https://core.telegram.org/bots/api#sendchecklist)
  Bot for creating lists
