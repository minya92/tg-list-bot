# Telegram Task List Bot

Telegram бот для создания интерактивных чеклистов задач с использованием стандартного Telegram API (inline keyboard).

## Особенности

- ✅ **Интерактивные чеклисты** - нажимайте на задачи, чтобы отметить их выполненными
- ✅ **Inline keyboard** - используется стандартное Telegram API (работает везде!)
- ✅ **Автосохранение** - состояние чеклиста сохраняется для каждого пользователя
- ✅ **Сброс чеклиста** - кнопка для быстрого сброса всех задач
- ✅ **Без Premium** - работает на любом аккаунте Telegram

## Установка

1. Клонируйте репозиторий:

```bash
git clone <repo-url>
cd tg-list-bot
```

2. Установите зависимости:

```bash
npm install
```

3. Создайте файл `.env` в корне проекта и добавьте токен вашего бота:

```
BOT_TOKEN=your_telegram_bot_token_here
```

## Как получить токен бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания нового бота
4. Скопируйте полученный токен и добавьте его в `.env` файл

## Запуск

### Режим разработки (с автоматической перекомпиляцией):

```bash
npm run dev
```

### Продакшн:

```bash
npm run build
npm start
```

## 🚀 Развертывание на VDS

Для автоматического развертывания на VDS сервере с помощью GitHub Actions:

- 📖 **Полная инструкция**: [DEPLOYMENT.md](DEPLOYMENT.md)
- ⚡ **Быстрый старт**: [QUICK_DEPLOY.md](QUICK_DEPLOY.md)

После настройки бот будет автоматически обновляться на сервере при каждом push в ветку `main`.

## Использование

### Команды бота:

- `/start` - Приветствие и проверка статуса подключения
- `/debug` - Проверить статус Business подключения
- `/list XXX` - Создать список задач (где XXX - номер задачи)

### Пример:

```
/list 1234
```

Бот отправит список задач с заголовком:

```
#task 1234. https://sfera-t1.ru/tasks/task/TCOMCLOUD-1234
```

И список из 18 задач для вашего рабочего процесса.

### 💡 Режимы работы:

Бот использует **Telegram Inline Keyboard** - стандартное API, которое работает везде:

1. Отправляете команду `/list 1234`
2. Получаете интерактивный чеклист с кнопками
3. Нажимаете на задачу - она отмечается выполненной (✅)
4. Нажимаете снова - отмечается невыполненной (⬜️)
5. Кнопка "🔁 Сбросить чеклист" - для быстрого сброса

**Преимущества:**

- ✅ Работает на любом аккаунте (не требуется Premium)
- ✅ Интерактивность - реальные кнопки Telegram
- ✅ Визуальная обратная связь
- ✅ Сохранение состояния для каждого пользователя

## Структура проекта

```
tg-list-bot/
├── src/
│   └── index.ts          # Основной файл бота
├── dist/                 # Скомпилированные файлы (создается после build)
├── .env                  # Переменные окружения (не в репозитории)
├── .gitignore
├── package.json
├── tsconfig.json
└── README.md
```

## Технические детали

### Структура Checklist

Бот использует следующие типы данных из Telegram Bot API:

**InputChecklistTask:**

```typescript
interface InputChecklistTask {
  text: string; // Текст задачи
  checked?: boolean; // Статус: выполнена (true) или нет (false)
}
```

**InputChecklist:**

```typescript
interface InputChecklist {
  title?: string; // Заголовок списка
  tasks: InputChecklistTask[]; // Массив задач
}
```

### API Request

Бот отправляет POST-запрос на:

```
https://api.telegram.org/bot<TOKEN>/sendChecklist
```

С телом:

```json
{
  "chat_id": <chat_id>,
  "checklist": {
    "title": "#task XXX. https://sfera-t1.ru/tasks/task/TCOMCLOUD-XXX",
    "tasks": [
      { "text": "1. Передвинуть задачу в Сфере", "checked": false },
      ...
    ]
  }
}
```

## Список задач

Бот отправляет следующий хардкодированный список задач:

1. Передвинуть задачу в Сфере
2. Создать ветку от релизной
3. Выполнить задачу
4. Обновить сторибук
5. Обновить тесты (npm test -- -u ./src/components/MyComponent)
6. Поднять версии компонентов и сбилдить их
7. Залить компоненты на дев
8. Собрать страницу
9. Проверить версии компонентов и Комит
10. ПР в дев
11. Код-ревью
12. Написать в задаче ссыль на п.3 и версии компонентов
13. Мёрдж в дев
14. Поменять в баге исполнителя либо закрыть задачу
15. Списать время с задачи в Сфере
16. Написать тестеру
17. Создать пр в релизную ветку со статусами тестирование не вливать и проверить название
18. После тестирования поменять статус

## Ссылки

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Telegraf Documentation](https://telegraf.js.org/)
- [Inline Keyboards](https://core.telegram.org/bots/features#inline-keyboards)
  Bot for creating lists
