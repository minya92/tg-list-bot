# 🚀 Инструкция по развертыванию бота на VDS

## 📋 Предварительные требования

- VDS сервер с Ubuntu/Debian
- SSH доступ (IP, имя пользователя, пароль)
- Репозиторий на GitHub

## 🔧 Первоначальная настройка сервера

### 1. Подключитесь к серверу через SSH

```bash
ssh ваш_пользователь@ваш_ip_адрес
```

### 2. Установите необходимое ПО

#### Вариант A: Через nvm (рекомендуется)

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Git
sudo apt install -y git

# Установка nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# Перезагрузите оболочку или выполните:
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

# Установка Node.js через nvm
nvm install 20
nvm use 20
nvm alias default 20

# Проверка установки
node --version
npm --version

# Установка PM2 глобально
npm install -g pm2
```

#### Вариант B: Системная установка Node.js

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Node.js (версия 20.x LTS)
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# Проверка установки
node --version
npm --version

# Установка Git
sudo apt install -y git

# Установка PM2 глобально
sudo npm install -g pm2
```

### 3. Клонируйте репозиторий

```bash
# Перейдите в домашнюю директорию
cd ~

# Клонируйте репозиторий
git clone https://github.com/ваш_пользователь/tg-list-bot.git

# Перейдите в папку проекта
cd tg-list-bot
```

### 4. Настройте переменные окружения

```bash
# Создайте .env файл
nano .env
```

Добавьте следующее содержимое:

```env
BOT_TOKEN=ваш_токен_бота_от_BotFather
```

Сохраните файл (Ctrl+O, Enter, Ctrl+X).

### 5. Соберите и запустите бота

```bash
# Установите зависимости
npm install

# Соберите проект
npm run build

# Создайте директорию для логов
mkdir -p logs

# Запустите бота через PM2
pm2 start ecosystem.config.js

# Сохраните конфигурацию PM2
pm2 save

# Настройте автозапуск PM2 при перезагрузке сервера
pm2 startup
# Выполните команду, которую покажет PM2
```

### 6. Проверьте статус бота

```bash
# Посмотреть статус
pm2 status

# Посмотреть логи
pm2 logs tg-list-bot

# Посмотреть последние логи
pm2 logs tg-list-bot --lines 50
```

## 🔐 Настройка GitHub Secrets

Для автоматического деплоя нужно добавить секреты в GitHub:

1. Перейдите в ваш репозиторий на GitHub
2. Откройте **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret** и добавьте следующие секреты:

| Название       | Описание                 | Пример              |
| -------------- | ------------------------ | ------------------- |
| `VDS_HOST`     | IP адрес вашего VDS      | `123.45.67.89`      |
| `VDS_USERNAME` | Имя пользователя для SSH | `root` или `ubuntu` |
| `VDS_PASSWORD` | Пароль для SSH           | `ваш_пароль`        |

## ✅ Готово!

Теперь при каждом коммите в ветку `main`:

1. 🛑 Бот автоматически остановится
2. 📥 Подтянутся новые изменения
3. 📦 Установятся зависимости
4. 🔨 Проект пересоберется
5. ✅ Бот запустится заново

## 📊 Полезные команды PM2

```bash
# Просмотр статуса
pm2 status

# Просмотр логов в реальном времени
pm2 logs tg-list-bot

# Перезапуск бота
pm2 restart tg-list-bot

# Остановка бота
pm2 stop tg-list-bot

# Удаление бота из PM2
pm2 delete tg-list-bot

# Просмотр подробной информации
pm2 show tg-list-bot

# Очистка логов
pm2 flush
```

## 🔍 Проверка деплоя

После push в GitHub:

1. Перейдите в **Actions** в вашем репозитории на GitHub
2. Проверьте статус последнего workflow
3. Зеленая галочка ✅ = деплой успешен
4. Красный крестик ❌ = произошла ошибка (проверьте логи)

## 🐛 Решение проблем

### Бот не запускается

```bash
# Проверьте логи
pm2 logs tg-list-bot --lines 100

# Проверьте, что .env файл существует
cat .env

# Проверьте, что проект собран
ls -la dist/
```

### Проблемы с правами доступа

```bash
# Установите правильные права на папку
chmod -R 755 ~/tg-list-bot
```

### userChecklists.json не сохраняется

```bash
# Убедитесь, что файл существует и доступен для записи
touch ~/tg-list-bot/dist/userChecklists.json
chmod 644 ~/tg-list-bot/dist/userChecklists.json
```

## 🔒 Рекомендации по безопасности

1. **Используйте SSH ключи вместо пароля** (более безопасно)
2. **Настройте firewall**:
   ```bash
   sudo ufw allow 22/tcp  # SSH
   sudo ufw enable
   ```
3. **Регулярно обновляйте систему**:
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```
4. **Не храните пароли в коде** - используйте `.env` файл

## 📝 Дополнительная настройка (опционально)

### Использование SSH ключей вместо пароля

Это более безопасный метод:

```bash
# На вашем локальном компьютере:
ssh-keygen -t rsa -b 4096

# Скопируйте ключ на сервер:
ssh-copy-id ваш_пользователь@ваш_ip_адрес
```

Затем в GitHub Secrets вместо `VDS_PASSWORD` используйте `VDS_KEY` с содержимым приватного ключа.

Обновите `.github/workflows/deploy.yml`:

```yaml
- name: 🚀 Deploy to VDS
  uses: appleboy/ssh-action@v1.0.0
  with:
    host: ${{ secrets.VDS_HOST }}
    username: ${{ secrets.VDS_USERNAME }}
    key: ${{ secrets.VDS_KEY }} # вместо password
    port: 22
    script: |
      # ... остальной скрипт
```
